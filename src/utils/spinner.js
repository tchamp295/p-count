import { ClassicSpinner, FlapperSpinner } from 'react-spinners-kit';

export const LoadingSpinner = () => (
  <div className="flex items-center justify-center h-32">
<FlapperSpinner size={30} color="#4B49AC" />
  </div>
);
